import styles from './Title.module.scss'

const Title = () => {
  return (
    <div className={styles.deals__pageTitleContainer}>
      <span className={styles.deals__pageTitle}>Deals Management</span>
    </div>
  )
}

export default Title
